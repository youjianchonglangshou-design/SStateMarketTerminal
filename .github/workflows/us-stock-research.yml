name: Retired US Stock News Batch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired in v0.1.31
        run: |
          echo "US-stock news batch is retired."
          echo "News research is now user-triggered per symbol from the S3 / S0.5 / S1 card and runs directly through Cloudflare Worker."
